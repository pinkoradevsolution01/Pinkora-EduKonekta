'use client';
import { useEffect, useState } from 'react';
import { WorkspaceNav } from '../../components/workspace-nav';
const api = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:4000/api/v1';
type Overview = { activeUsers:number; classes:{total:number;activeEnrollments:number}; attendance:Array<{state:string;total:number}>; announcementReach:{reads:number;parentAcknowledgements:number}; assignmentCompletion:{pending:number}; safetyTotals:Array<{status:string;total:number}>; openGuidanceCases:number };
export default function AdminDashboardPage(){const[schoolId,setSchoolId]=useState('');const[data,setData]=useState<Overview|null>(null);const[error,setError]=useState('');useEffect(()=>{if(!schoolId)return;fetch(`${api}/dashboard/admin/overview`,{credentials:'include',headers:{'x-school-id':schoolId}}).then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x?.error?.message??'Unable to load dashboard');return x}).then(setData).catch(e=>setError(e.message))},[schoolId]);return <main className="brand-gradient min-h-screen px-5 py-10 text-slate-900 sm:px-8"><div className="mx-auto max-w-6xl"><WorkspaceNav/><h1 className="mt-8 text-3xl font-bold text-[#092d83]">School administration dashboard</h1><p className="mt-2 text-slate-600">Tenant-scoped totals. Safety and guidance data is aggregated and de-identified.</p><label className="mt-6 block max-w-xl text-sm font-medium">Active school ID<input value={schoolId} onChange={e=>setSchoolId(e.target.value)} className="mt-1 w-full rounded-xl border bg-white p-3" placeholder="School UUID"/></label>{error&&<p role="alert" className="mt-4 rounded bg-red-50 p-3 text-red-700">{error}</p>}{data&&<><section aria-label="Key school metrics" className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Active users',data.activeUsers],['Classes',data.classes.total],['Enrollments',data.classes.activeEnrollments],['Open guidance',data.openGuidanceCases]].map(([l,v])=><article key={String(l)} className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="text-sm text-slate-600">{l}</h2><p className="mt-2 text-3xl font-bold">{v}</p></article>)}</section><section className="mt-8 grid gap-6 lg:grid-cols-2"><MetricTable title="Attendance summary" rows={data.attendance.map(x=>[x.state,x.total])}/><MetricTable title="De-identified safety totals" rows={data.safetyTotals.map(x=>[x.status,x.total])}/></section><section className="mt-8 rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-semibold">Engagement and assignments</h2><dl className="mt-3 grid gap-3 sm:grid-cols-3"><div><dt>Announcement reads</dt><dd className="text-2xl font-bold">{data.announcementReach.reads}</dd></div><div><dt>Parent acknowledgements</dt><dd className="text-2xl font-bold">{data.announcementReach.parentAcknowledgements}</dd></div><div><dt>Assignments pending</dt><dd className="text-2xl font-bold">{data.assignmentCompletion.pending}</dd></div></dl></section></>}</div></main>}
function MetricTable({title,rows}:{title:string;rows:Array<(string|number)[]>}){return <section className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-semibold">{title}</h2><table className="mt-3 w-full text-left"><caption className="sr-only">{title}</caption><thead><tr><th scope="col">Category</th><th scope="col">Total</th></tr></thead><tbody>{rows.map(([label,total])=><tr key={String(label)} className="border-t"><th scope="row" className="py-2 font-medium">{label}</th><td className="py-2">{total}</td></tr>)}</tbody></table></section>}
