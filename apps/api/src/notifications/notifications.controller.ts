import {Body,Controller,Get,Param,Patch,Post,Req,UseGuards} from '@nestjs/common';import{AuthGuard,TenantGuard}from'../auth/auth.guards';import{AuthenticatedRequest}from'../auth/auth.types';import{NotificationsService}from'./notifications.service';import{z}from'zod';
@Controller({path:'notifications',version:'1'})@UseGuards(AuthGuard,TenantGuard)export class NotificationsController{constructor(private s:NotificationsService){}@Get()list(@Req()r:AuthenticatedRequest){return this.s.list(r.auth!)}@Patch(':id/read')read(@Req()r:AuthenticatedRequest,@Param('id')id:string){return this.s.read(r.auth!,id)}@Get('preferences/me')prefs(@Req()r:AuthenticatedRequest){return this.s.preferences(r.auth!)}@Patch('preferences/:eventType')pref(@Req()r:AuthenticatedRequest,@Param('eventType')eventType:string,@Body()b:unknown){const x=z.object({inAppEnabled:z.boolean(),emailEnabled:z.boolean()}).parse(b);return this.s.setPreference(r.auth!,eventType,x.inAppEnabled,x.emailEnabled)}}
