import { ForbiddenException } from '@nestjs/common'; import { RoleCode } from '@prisma/client'; import { SchoolsService } from './schools.service';
const platform={userId:'platform',schoolId:null,roles:[RoleCode.PLATFORM_ADMIN]} as any; const schoolAdmin={userId:'admin-a',schoolId:'school-a',roles:[RoleCode.SCHOOL_ADMIN]} as any;
describe('SchoolsService authorization matrix',()=>{it('allows only a platform administrator to register a school',async()=>{const create=jest.fn().mockResolvedValue({id:'school-a'});const service=new SchoolsService({school:{create}} as any);await service.create(platform,{name:'School A',slug:'school-a'});expect(create).toHaveBeenCalled();await expect(service.create(schoolAdmin,{name:'School B',slug:'school-b'})).rejects.toBeInstanceOf(ForbiddenException)});it('rejects a school administrator invitation for another school',async()=>{const service=new SchoolsService({}as any);await expect(service.invite(schoolAdmin,{schoolId:'school-b',email:'parent@example.test',role:RoleCode.PARENT})).rejects.toBeInstanceOf(ForbiddenException)});it('defends against an internal platform-role invite attempt',async()=>{const service=new SchoolsService({}as any);await expect(service.invite(platform,{schoolId:'school-a',email:'admin@example.test',role:RoleCode.PLATFORM_ADMIN} as any)).rejects.toBeInstanceOf(ForbiddenException)})});
